import React from 'react';
import { Typography } from '@mui/material';

const SocialLinksStep = ({ formData, setFormData }) => {
    return (
        <div>
            <Typography variant="h6">Social Media Profile</Typography>
            {/* We will add the form fields here later */}
        </div>
    );
};

export default SocialLinksStep;